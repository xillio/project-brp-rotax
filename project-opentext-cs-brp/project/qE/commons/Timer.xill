use Date;function start() {    var log = {};    log.startTime = Date.now();        return log;}function end(log) {    log.endTime = Date.now();    log.time = Date.diff(log.endTime, log.startTime).totalMillis;        return log;}function avgSpeed(timeObj, items) {    if( !timeObj.startTime ){ return; }        return items/(end(timeObj).time/1000) :: " items per sec";}function getTiming(timeObj) {    if( !timeObj.startTime ){ return; }        return (end(timeObj).time/1000);}